<% if @achievement.valid? %>
	$('#achievements').append(<%= render(:partial => @achievement).inspect %>);
	$('#new_achievement')[0].reset();
<% end %>
$("#new_achievement").show_errors(<%= errors_to_json(@achievement) %>);
